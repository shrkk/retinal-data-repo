---
phase: 01-security-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test_db.py
  - .gitignore
  - .env.example
autonomous: false
requirements:
  - DATA-01

must_haves:
  truths:
    - "test_db.py contains no database password or connection string with credentials"
    - "The leaked password has been purged from all git history"
    - ".gitignore includes .env to prevent future credential commits"
  artifacts:
    - path: "test_db.py"
      provides: "Credential-free test script using env var"
      contains: "os.environ"
    - path: ".gitignore"
      provides: "Git ignore rules for secrets"
      contains: ".env"
    - path: ".env.example"
      provides: "Template for required environment variables"
      contains: "DATABASE_URL"
  key_links:
    - from: "test_db.py"
      to: "DATABASE_URL env var"
      via: "os.environ.get"
      pattern: 'os\.environ'
---

<objective>
Remove the committed Supabase database password from test_db.py and purge it from all git history, then verify the repository is clean.

Purpose: The file test_db.py contains a live Supabase PostgreSQL password in plaintext (line 4). This is a security emergency that must be resolved before any other work proceeds. The password must be rotated in Supabase after purging.

Output: A clean git history with no credentials, test_db.py reading DATABASE_URL from environment, and confirmation that the user has rotated the password in Supabase dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip credentials from test_db.py and purge git history</name>
  <files>test_db.py, .gitignore, .env.example</files>
  <read_first>
    - test_db.py (current file with hardcoded password on line 4)
    - .gitignore (current gitignore rules)
  </read_first>
  <action>
1. Rewrite test_db.py to remove the hardcoded connection string. The new file should:
   - Import `os` and `asyncio` and `asyncpg`
   - Read the connection URL from environment: `DATABASE_URL = os.environ.get("DATABASE_URL")`
   - Add a guard: `if not DATABASE_URL: raise RuntimeError("DATABASE_URL environment variable is not set")`
   - Keep the same `test_connection()` async function structure but use the env-var-sourced `DATABASE_URL`
   - Keep `ssl="require"` in the `asyncpg.connect()` call
   - Remove the entire hardcoded string `postgresql://postgres.ytglrmyvdzhidwcutrtn:REDACTED_PASSWORD@aws-1-us-east-2.pooler.supabase.com:5432/postgres`

2. Verify `.gitignore` already contains `.env` — it does (confirmed from read). No change needed there.

3. Purge the credential from git history using `git filter-repo`:
   - First, install git-filter-repo if not available: `pip install git-filter-repo` or `brew install git-filter-repo`
   - Run: `git filter-repo --replace-text <(echo 'REDACTED_PASSWORD==>REDACTED_PASSWORD') --force`
   - This replaces every occurrence of the password string in all commits with `REDACTED_PASSWORD`
   - Alternative if git-filter-repo is unavailable: use BFG Repo-Cleaner or `git filter-branch`

4. After history rewrite, verify no trace remains:
   - Run: `git log --all -p | grep -c "REDACTED_PASSWORD"` — must return 0
   - Run: `grep -r "REDACTED_PASSWORD" . --include="*.py"` — must return 0

5. Create a `.env.example` file at repo root with placeholder values:
   ```
   DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
   ADMIN_PASSWORD=change-me-to-a-random-32-char-string
   ALLOWED_ORIGINS=http://localhost:5173
   ```
  </action>
  <verify>
    <automated>git log --all -p 2>/dev/null | grep -c "REDACTED_PASSWORD" | grep -q "^0$" && grep -q "os.environ" test_db.py && ! grep -q "REDACTED_PASSWORD" test_db.py && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - test_db.py contains `os.environ.get("DATABASE_URL")` (no hardcoded password)
    - `grep -r "REDACTED_PASSWORD" . --include="*.py"` returns no matches
    - `git log --all -p | grep -c "REDACTED_PASSWORD"` returns 0
    - `.env.example` exists at repo root with DATABASE_URL, ADMIN_PASSWORD, ALLOWED_ORIGINS placeholders
  </acceptance_criteria>
  <done>test_db.py reads DATABASE_URL from environment. The password string "REDACTED_PASSWORD" appears nowhere in the repository or its git history. .env.example documents required env vars.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Rotate Supabase database password</name>
  <files>none (Supabase dashboard action)</files>
  <action>
User must rotate the database password in the Supabase dashboard because the old password was committed to git and is compromised. Claude cannot access the Supabase dashboard to perform this action.
  </action>
  <what-built>The credential has been purged from the repository and git history. test_db.py now reads from DATABASE_URL env var.</what-built>
  <how-to-verify>
    1. Go to the Supabase Dashboard: https://supabase.com/dashboard
    2. Select the project (ref: ytglrmyvdzhidwcutrtn)
    3. Navigate to Settings -> Database
    4. Click "Reset database password" and generate a new password
    5. Copy the new password
    6. Update your local .env file with the new DATABASE_URL using the new password:
       DATABASE_URL=postgresql://postgres.ytglrmyvdzhidwcutrtn:[NEW_PASSWORD]@aws-0-us-east-2.pooler.supabase.com:6543/postgres
       (Note: use port 6543 for transaction pooler, not 5432)
    7. Test connection: `source .env && python test_db.py`
    8. Confirm it prints "Connection successful"
  </how-to-verify>
  <verify>
    <automated>source .env 2>/dev/null && python test_db.py 2>&1 | grep -q "Connection successful" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .env file exists with DATABASE_URL containing the NEW rotated password (not the old REDACTED_PASSWORD)
    - `python test_db.py` prints "Connection successful" using the new password
  </acceptance_criteria>
  <done>The Supabase database password has been rotated. The old compromised password no longer works. The new password is stored in .env (gitignored) and test_db.py connects successfully.</done>
  <resume-signal>Type "password rotated" after completing the steps above, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "REDACTED_PASSWORD" . --include="*.py"` returns nothing
- `git log --all -p | grep "REDACTED_PASSWORD"` returns nothing
- `cat test_db.py` shows `os.environ.get("DATABASE_URL")`, no hardcoded strings
- `.env.example` exists with placeholder values
- User confirms password rotation in Supabase dashboard
- `python test_db.py` succeeds with rotated password
</verification>

<success_criteria>
1. The password string does not appear anywhere in the repository files or git history
2. test_db.py uses DATABASE_URL from environment and refuses to run without it
3. The Supabase password has been rotated (confirmed by user)
4. A new .env file with the rotated password connects successfully via `python test_db.py`
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-foundation/01-01-SUMMARY.md`
</output>
